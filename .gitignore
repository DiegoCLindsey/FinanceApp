node_modules/
dist/
# El BUNDLE SÍ se versiona (ver docs/02-plan-refactor.md, P.7): GitHub Pages
# puede estar sirviendo la rama en vez del artefacto del workflow, y entonces un
# fichero solo generado en CI da 404 y tumba la aplicación entera. Un paso de CI
# recompila y compara para que no pueda desincronizarse del fuente.
# El sourcemap no: son 900 KB que nadie necesita en producción.
assets/financeapp-core.js.map
coverage/
_site/
.DS_Store
*.local
.env.local
.env.*.local
firebase/firebase-config.js
.claude/

# Artefactos del banco de QA (ver tools/qa/README.md)
qa.html
tools/qa/vendor/
